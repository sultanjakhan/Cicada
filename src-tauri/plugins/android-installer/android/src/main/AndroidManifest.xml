<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
  <!-- Health Connect is optional; its bridge is loaded only on API 28+. -->
  <uses-sdk tools:overrideLibrary="androidx.health.connect.client" />
  <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
  <!-- API 31+: lets an installer of record ask PackageInstaller to update itself
       without a confirmation sheet. The OS may still return PENDING_USER_ACTION. -->
  <uses-permission android:name="android.permission.UPDATE_PACKAGES_WITHOUT_USER_ACTION" />

  <uses-permission android:name="android.permission.health.READ_SLEEP" />
  <uses-permission android:name="android.permission.health.READ_EXERCISE" />
  <uses-permission android:name="android.permission.health.READ_STEPS" />
  <uses-permission android:name="android.permission.health.READ_HEALTH_DATA_IN_BACKGROUND" />
  <queries><package android:name="com.google.android.apps.healthdata" /></queries>
  <application>
    <activity android:name="app.hanni.mvp.android.installer.HealthSleepRationaleActivity" android:exported="true">
      <intent-filter><action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" /></intent-filter>
    </activity>
    <activity-alias android:name="app.hanni.mvp.android.installer.HealthSleepPermissionUsage"
      android:targetActivity="app.hanni.mvp.android.installer.HealthSleepRationaleActivity"
      android:exported="true" android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
      <intent-filter>
        <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
        <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
      </intent-filter>
    </activity-alias>
    <receiver
      android:name="app.hanni.mvp.android.installer.HanniUpdateResultReceiver"
      android:exported="false" />
    <provider
      android:name="app.hanni.mvp.android.installer.HanniUpdateFileProvider"
      android:authorities="${applicationId}.android-installer"
      android:exported="false"
      android:grantUriPermissions="true">
      <meta-data
        android:name="android.support.FILE_PROVIDER_PATHS"
        android:resource="@xml/hanni_update_paths" />
    </provider>
  </application>
</manifest>
